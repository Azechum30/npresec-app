import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Teachers",
};

export default function TeachersPage() {
  return <div>Teachers perform their responibilities here!</div>;
}
